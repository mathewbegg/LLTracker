export interface CharacterUI {
    rank: number;
    name: string;
    description: string;
    pictureURL?: string;
}

export interface CharacterMetaInfo {
    quantity: number;
    characterName: string;
    pictureURL?: string;
}